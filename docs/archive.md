# Архивирование таблицы

## Описание

Функционал позволяет создать полную копию таблицы (архив) и очистить накопительные листы в основной таблице для продолжения работы.

## Как использовать

1. Откройте таблицу в Google Sheets
2. В меню выберите: **⚙️ Настройки скрипта** → **🗄️ Архивировать и очистить таблицу**
3. В диалоговом окне ознакомьтесь с информацией и нажмите "Да"
4. Дождитесь завершения процесса

## Что происходит при архивировании

### Шаг 1: Создание архива
- Создается полная копия таблицы в той же папке Google Drive
- Имя архива: `[Имя таблицы]_Archive_до_YYYY-MM-DD`
- Архив — независимый файл со всеми данными на момент архивирования

### Шаг 2: Очистка листов
После создания архива очищаются накопительные листы в **основной** таблице.

## Какие листы очищаются

| Лист | Действие |
|------|----------|
| Ежедневные ф отчеты | Удаляются все строки кроме заголовков |
| Лента заказов | Удаляются все строки кроме заголовков |
| Аналитика РК | Удаляются все строки кроме заголовков |
| Аналитика продавца | Удаляются все строки кроме заголовков |
| Воронка динамика | Удаляются только столбцы данных (дни и недели), структура сохраняется |

## Какие листы НЕ очищаются

| Лист | Причина |
|------|---------|
| ID-ART | Справочник (ручное заполнение) |
| История рекламных расходов | Сохраняется по требованию |
| воронка отчет | Сохраняется по требованию |
| Процентная разбивка размеров | Сохраняется по требованию |
| Остатки | Всегда содержит актуальные данные |

## Структура файлов после архивирования

```
📁 Google Drive / Ваша папка
├── 📊 WB_Analytics              ← Основная таблица (очищенная)
├── 📊 WB_Analytics_Archive_до_2025-01-22
├── 📊 WB_Analytics_Archive_до_2024-07-15
└── 📊 WB_Analytics_Archive_до_2024-01-10
```

## Когда рекомендуется архивировать

- Таблица стала медленно работать
- Накопилось данных за 6+ месяцев
- Перед существенными изменениями в структуре

## Технические детали

- Файл: `archive.gs`
- Главная функция: `showArchiveConfirmDialog()`
- Копирование через: `DriveApp.getFileById().makeCopy()`

## Ограничения

- Требуется разрешение на доступ к Google Drive
- Время выполнения зависит от размера таблицы (обычно 1-5 минут)
- Архив создается в той же папке, что и основная таблица

## Безопасность

- Архив создается **ДО** очистки листов
- Если произойдет ошибка при очистке, данные сохранены в архиве
- Архивы не удаляются автоматически
