# Синхронизация отчетов за выбранную дату

## Описание

Модуль для загрузки отчетов Wildberries за любую конкретную дату через удобный графический интерфейс.

## Возможности

- 📅 Выбор любой даты для загрузки отчетов
- ✅ Выборочная синхронизация отчетов (можно выбрать только нужные)
- 🚀 Быстрая загрузка данных за прошлые периоды
- 📊 Заполнение пропущенных дат в истории
- 🔄 Повторная загрузка данных при необходимости

## Как использовать

### Через меню Google Таблиц

1. Откройте вашу Google Таблицу с данными Wildberries
2. В меню выберите: **⚙️ Настройки скрипта → 📅 Синхронизация за дату...**
3. В открывшемся окне:
   - Выберите дату отчета (по умолчанию вчерашний день)
   - Отметьте галочками нужные отчеты
   - Нажмите кнопку **"Синхронизировать"**
4. Дождитесь завершения загрузки
5. Окно автоматически закроется после успешной синхронизации

### Доступные отчеты для выбора

| Отчет | Описание | По умолчанию |
|-------|----------|--------------|
| **Остатки товаров** | Текущие остатки на складах | ❌ |
| **Ежедневные финансовые отчеты** | Детализация продаж, возвратов, комиссий | ✅ |
| **Лента заказов** | Информация о заказах | ✅ |
| **Аналитика РК** | Статистика рекламных кампаний | ✅ |
| **История рекламных расходов** | Фактические затраты на рекламу | ✅ |
| **Аналитика продавца** | Воронка продаж по товарам | ✅ |
| **Воронка динамика** | Добавить столбец с метриками за дату | ❌ |

## Особенности работы

### Проверка дубликатов

Скрипт автоматически проверяет наличие данных за выбранную дату:
- Если данные уже загружены, они **не будут загружены повторно**
- Это защищает от дублирования записей в таблице

### Зависимости данных

**Аналитика продавца** требует сравнения с данными год назад:
- При выборе даты 20.01.2026, скрипт также запросит данные за 20.01.2025
- Убедитесь, что за прошлогоднюю дату есть данные в WB API

### Остатки товаров

**Важно:** Отчет "Остатки товаров" не зависит от даты:
- Всегда загружаются текущие остатки
- Полностью перезаписывают предыдущие данные
- Обычно не нужно выбирать для исторической загрузки

### Воронка динамика

**Особенность:** Добавление столбца с метриками:
- Столбец вставляется в хронологическом порядке
- Если дата уже есть в таблице, новый столбец не добавляется
- Полезно для заполнения пропущенных дат
- Можно использовать отдельно от других отчетов

## Примеры использования

### Заполнение пропущенной даты

Если забыли запустить синхронизацию вчера:

1. Откройте **📅 Синхронизация за дату...**
2. Выберите пропущенную дату
3. Отметьте все нужные отчеты (включая "Воронка динамика")
4. Запустите синхронизацию
5. Столбец автоматически встанет на нужное место между существующими датами

### Загрузка данных за прошлый месяц

Для загрузки исторических данных:

1. Запускайте синхронизацию для каждой даты отдельно
2. Начните с самой ранней даты
3. Двигайтесь к более поздним датам
4. Для ускорения можно снять галочки с ненужных отчетов

### Обновление данных

Если данные в WB изменились (например, обновились заказы):

1. Выберите нужную дату
2. Отметьте только те отчеты, которые нужно обновить
3. **Сначала удалите** старые данные за эту дату из таблицы вручную
4. Запустите синхронизацию

## Интерфейс

### Элементы управления

**Поле даты:**
- Календарь для выбора даты
- По умолчанию установлен вчерашний день
- Можно ввести дату вручную в формате ДД.MM.ГГГГ

**Чекбоксы отчетов:**
- "Выбрать все отчеты" - быстрый выбор всех отчетов
- Индивидуальные чекбоксы для каждого отчета

**Кнопки:**
- "Отмена" - закрыть окно без изменений
- "Синхронизировать" - запустить загрузку данных

### Статусы выполнения

Во время синхронизации отображаются сообщения:
- 🔵 Синий фон: процесс выполняется
- ✅ Зеленый фон: успешное завершение
- ❌ Красный фон: ошибка выполнения

## Технические детали

### Структура модуля

**Файлы:**
- `dateSelector.html` - HTML интерфейс выбора даты
- `syncByDate.gs` - функции синхронизации с параметром даты
- `Code.gs` - основная функция обработки запроса

**Основные функции:**
```javascript
// Открытие диалога
showDateSelectorDialog()

// Синхронизация за дату
syncReportsByDate(date, reports)

// Функции для каждого отчета
syncFinanceDailyReportByDate(date)
syncOrdersFeedByDate(date)
syncAdsAnalyticsByDate(date)
syncAdsCostsByDate(date)
syncSalesFunnelByDate(date)
```

### Параметры функции syncReportsByDate

**Входные параметры:**
- `date` (string) - дата в формате YYYY-MM-DD
- `reports` (array) - массив кодов отчетов ['financeDaily', 'ordersFeed', ...]

**Возвращаемое значение:**
```javascript
{
  success: true/false,
  message: "строка с результатом",
  results: ["массив успешно загруженных отчетов"],
  errors: ["массив отчетов с ошибками"]
}
```

### Логирование

Все операции записываются в журнал Apps Script:
- Начало и конец синхронизации
- Обработка каждого отчета
- Количество загруженных строк
- Ошибки и предупреждения

Просмотр логов: **Apps Script → Выполнение → Просмотр журналов выполнения**

## Ограничения

1. **Лимиты API Wildberries:**
   - Соблюдаются автоматически (паузы между запросами)
   - Большие периоды лучше загружать частями

2. **Проверка дубликатов:**
   - Работает по ключевым полям (дата, ID товара/кампании)
   - Невозможно загрузить одни и те же данные дважды без ручного удаления

3. **Время выполнения:**
   - Зависит от объема данных
   - Может занять несколько минут для всех отчетов
   - Apps Script имеет лимит выполнения ~6 минут

## Часто задаваемые вопросы

**Q: Можно ли загрузить данные за несколько дат сразу?**  
A: Нет, интерфейс поддерживает загрузку только за одну дату. Для множественной загрузки запускайте синхронизацию несколько раз.

**Q: Что делать, если данные за дату уже загружены?**  
A: Скрипт автоматически пропустит уже существующие данные. Для обновления нужно сначала удалить старые данные вручную.

**Q: Почему отчеты "Остатки" и "Воронка динамика" выключены по умолчанию?**  
A: Остатки не зависят от даты и всегда показывают текущее состояние. Воронка динамика - это дополнительный шаг, который обычно запускается отдельно после загрузки основных отчетов.

**Q: Что произойдет, если столбец с такой датой уже есть в Воронке динамика?**  
A: Скрипт автоматически обнаружит существующий столбец и пропустит добавление, чтобы избежать дублирования.

**Q: В каком порядке добавляются столбцы в Воронку динамика?**  
A: Столбцы всегда располагаются в хронологическом порядке (слева направо, от старых дат к новым). При добавлении пропущенной даты столбец автоматически вставится в нужное место.

**Q: Можно ли загрузить данные за сегодняшний день?**  
A: Технически да, но данные могут быть неполными, так как день еще не закончился. Рекомендуется загружать данные за прошедшие дни.

**Q: Сколько времени хранятся данные в API Wildberries?**  
A: Зависит от типа отчета. Обычно доступны данные за последние 30-90 дней. Точные сроки уточняйте в документации WB API.

## Решение проблем

**Ошибка: "Данные за дату уже существуют"**
- Это не ошибка, а информация
- Данные уже загружены, повторная загрузка не требуется

**Ошибка: "Нет данных за дату"**
- Проверьте, что за эту дату есть данные в личном кабинете WB
- Убедитесь, что дата не слишком старая (данные могут быть удалены из API)

**Синхронизация зависла**
- Подождите до 6 минут (лимит выполнения Apps Script)
- Проверьте логи выполнения
- Попробуйте синхронизировать отчеты по отдельности

**Ошибка API токена**
- Проверьте настройки (⚙️ Настройки скрипта → Открыть настройки)
- Убедитесь, что токен не истек
- Проверьте права доступа токена в личном кабинете WB
