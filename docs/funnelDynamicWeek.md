# Недельная статистика для листа "Воронка динамика"

## Описание

Функционал автоматического добавления недельной статистики в лист "Воронка динамика". Позволяет:
- Добавлять дневные столбцы за всю неделю (7 дней)
- Автоматически создавать итоговый недельный столбец со средними значениями

## Структура недельного столбца

### Даты
- **Строка 7** - Дата начала недели (понедельник) в формате DD.MM.YYYY
- **Строка 8** - Дата окончания недели (воскресенье) в формате DD.MM.YYYY

### Средние значения за неделю
Следующие строки содержат средние значения за 7 дней (формула `=AVERAGE(AA:AG)` где AA-AG это 7 дневных столбцов):

- **Строка 9** - Среднее заказов по артикулу
- **Строка 11** - Среднее суммы заказов по артикулу
- **Строка 13** - Среднее заказов по модели
- **Строка 15** - Среднее суммы заказов по модели
- **Строка 17** - Среднее выкупов по артикулу
- **Строка 18** - Среднее выкупов по модели
- **Строка 27** - Среднее переходов в карточку
- **Строка 28** - Среднее показов рекламы по цвету
- **Строка 30** - Среднее кликов рекламы по цвету
- **Строка 31** - Среднее показов рекламы по модели
- **Строка 32** - Среднее кликов рекламы по модели
- **Строка 34** - Среднее добавлений в корзину
- **Строка 38** - Среднее затрат на рекламу по артикулу
- **Строка 39** - Среднее затрат на рекламу по модели

### Расчетные показатели
- **Строка 16** - Доля заказов артикула в группе: `=IFERROR(AH11/AH15,0)`
- **Строка 24** - % выкупа по артикулу: `=SUM(AA17:AG17)/SUM(AA9:AG9)`
- **Строка 25** - % выкупа по модели: `=AH18/AH13`
- **Строка 26** - CTR артикула: `=IFERROR(AH30/AH28,0)`
- **Строка 33** - CTR модели: `=IFERROR(AH32/AH31,0)`
- **Строка 35** - Конверсия в корзину: `=IFERROR(AH34/(AH27+AH30),0)`
- **Строка 36** - Конверсия в заказ: `=IFERROR(AH9/AH34,0)`
- **Строка 40** - CPO: `=IFERROR(AH38/AH9,0)`
- **Строка 41** - CPC по артикулу: `=IFERROR(AH38/AH30,0)`
- **Строка 42** - CPC по модели: `=IFERROR(AH39/AH32,0)`
- **Строка 43** - CPS по артикулу: `=IFERROR(AH38/(AH9*#REF!),0)`
- **Строка 44** - CPS по модели: `=IFERROR(AH39/(AH13*#REF!),0)`
- **Строка 45** - ДРР от заказа: `=IFERROR(AH38/AH11,0)`
- **Строка 46** - ДРР вмененная от выкупа: `=IFERROR(AH38/(AH11*#REF!),0)`
- **Строка 47** - ДРР от выкупа: `=IFERROR(AH38/#REF!,0)`
- **Строка 48** - ДРР от выкупа на модель: `=IFERROR(AH38/(AH15*#REF!),0)`
- **Строка 49** - ДРР модели: `=IFERROR(AH39/(AH15*#REF!),0)`
- **Строка 50** - Заказов на 1 клик: `=IFERROR(AH15/AH32,0)`

### Пустые строки
Следующие строки остаются пустыми для недельной статистики:
- Строки 10, 12, 14 - Динамика (только для дневных столбцов)
- Строки 19-23 - Детализация выкупов и возвратов
- Строка 29 - Динамика показов
- Строка 37 - Общий коэффициент

## Использование

### Через интерфейс HTML

1. Откройте меню **⚙️ Настройки скрипта** → **📅 Синхронизация за дату...**
2. Выберите дату (любой день недели или воскресенье)
3. Отметьте чекбокс **"Воронка динамика (добавить недельную статистику)"**
4. Нажмите **"Синхронизировать"**

Скрипт автоматически:
- Определит ближайшее воскресенье (конец недели)
- Добавит дневные столбцы за все 7 дней недели (если их еще нет)
- Создаст итоговый недельный столбец со средними значениями

### Программный вызов

```javascript
// Синхронизация недели, заканчивающейся указанной датой
syncSalesFunnelDynamicWeek('2024-01-21'); // воскресенье

// Или через общую функцию синхронизации
syncReportsByDate('2024-01-21', ['funnelDynamicWeek']);
```

## Функции

### `syncSalesFunnelDynamicWeek(weekEndDate)`
Главная функция синхронизации недельной статистики.

**Параметры:**
- `weekEndDate` (string) - Дата в формате YYYY-MM-DD (предпочтительно воскресенье)

**Возвращает:**
```javascript
{
  success: true,
  message: "Синхронизация недели завершена. Добавлено дневных столбцов: 7, недельный столбец: добавлен",
  daysAdded: 7,
  weekColumnAdded: true
}
```

**Поведение:**
- Если указана не воскресенье, автоматически корректирует на ближайшее следующее воскресенье
- Вычисляет дату понедельника (начало недели = воскресенье - 6 дней)
- Добавляет дневные столбцы для каждого дня недели (понедельник-воскресенье)
- Пропускает уже существующие столбцы
- Добавляет итоговый недельный столбец с формулами усреднения

### `addSalesFunnelDynamicWeekColumn(weekEndDate)`
Добавляет только недельный итоговый столбец (без дневных).

**Параметры:**
- `weekEndDate` (string) - Дата окончания недели (воскресенье) в формате YYYY-MM-DD

**Возвращает:**
```javascript
{
  success: true,
  message: "Недельный столбец успешно добавлен",
  skipped: false
}
```

### `addWeeklyColumn(sheet, col, startDate, endDate)`
Внутренняя функция для заполнения недельного столбца данными.

**Параметры:**
- `sheet` - Объект листа Google Sheets
- `col` (number) - Номер столбца для заполнения
- `startDate` (string) - Дата начала недели в формате YYYY-MM-DD
- `endDate` (string) - Дата окончания недели в формате YYYY-MM-DD

## Логика работы

### 1. Определение недели
```
Вход: любая дата (например, 2024-01-18, четверг)
↓
Находим ближайшее воскресенье: 2024-01-21
↓
Вычисляем понедельник: 2024-01-21 - 6 дней = 2024-01-15
↓
Неделя: 2024-01-15 (пн) ... 2024-01-21 (вс)
```

### 2. Поиск дневных столбцов
Скрипт ищет в строке 8 (дата до) столбцы, которые входят в диапазон недели:
```
Строка 8: | 15.01.2024 | 16.01.2024 | 17.01.2024 | ... | 21.01.2024 |
           ↑ столбец B   ↑ столбец C              ...   ↑ столбец H
```

### 3. Формирование формул
Для каждой строки со средним значением создается формула:
```
=AVERAGE(B9:H9)  // где B-H это найденные дневные столбцы за неделю
```

### 4. Хронологический порядок
Столбцы всегда вставляются в хронологическом порядке по дате окончания (строка 8).

## Требования

1. **Лист должен быть инициализирован** - минимум 50 строк с заголовками в столбце A
2. **Дневные столбцы должны существовать** - для корректного усреднения нужны столбцы за все 7 дней
3. **Формат дат** - все даты в формате YYYY-MM-DD для API и DD.MM.YYYY для отображения

## Примечания

- Формулы с `#REF!` зависят от внешних ячеек, которые должны быть настроены вручную
- Недельный столбец не перезаписывает существующие данные
- Если дневные столбцы не найдены, недельный столбец все равно создается, но формулы будут ссылаться на себя
- Рекомендуется сначала синхронизировать все дневные данные, а потом добавлять недельный столбец

## Автоматизация

Для автоматического добавления недельных столбцов каждое воскресенье:

```javascript
function weeklyTrigger() {
  var today = new Date();
  var dayOfWeek = today.getDay();
  
  // Запускаем только по воскресеньям
  if (dayOfWeek === 0) {
    var dateStr = Utilities.formatDate(today, Session.getScriptTimeZone(), 'yyyy-MM-dd');
    syncSalesFunnelDynamicWeek(dateStr);
  }
}
```

Настройте триггер в Google Apps Script:
- Тип: Временной
- Периодичность: Ежедневно
- Время: 23:00-00:00 (после выполнения всех дневных отчетов)
